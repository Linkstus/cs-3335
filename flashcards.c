#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char user[100];

    

    puts("Type end to end the program.");
    
    while(strcmp(user, "end") != 0 )
    {
        puts("Greetings User! Would you like to study or input questions? ");
        scanf("%s", user);

        if( Strcmp (user,"input") == 0)
        {
            
        }

    }
}